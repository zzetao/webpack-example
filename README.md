# webpack-example
Example project using Webpack

# Requirements
- webpack (npm install webpack --global)
- webpack-dev-server (npm install webpack-dev-server --global)

fisrt
```
npm install
```

then
```
npm run dev
```